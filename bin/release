#!/bin/sh

cat << EOF
---
addons:
  - heroku-postgresql
default_process_types:
  web: concurrently "yarn start ../../app-config.yaml --config ../../app-config.heroku.yaml" "yarn start-backend --config ../../app-config.yaml --config ../../app-config.heroku.yaml"
EOF